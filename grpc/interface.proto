syntax = "proto3";

package ping;
option go_package = "https://github.com/mfoman/go-ass-4;ping";


service Ping {
    rpc ping (Request) returns (Reply) {}
}

message Request {
    uint32 id = 1;
}

message Reply {
    uint32 amount = 1;
}
